import { Spinner } from '@/components/ui/spinner'

/**
 * Compatibility name for AI Elements' retired Loader component.
 * The upstream project now redirects Loader consumers to shadcn/ui Spinner.
 */
export const Loader = Spinner
